# 把這份內容存成 ~/.hermes/.env(或加進你既有的 .env),並把 <YOUR_TOKEN> 換成課程發給你的 LiteLLM token。
#
# ⚠️  token 是個人機密,不可 commit 進任何 public/private repo,也不可貼到 chat / issue。
# 本 starter 的 .gitignore 已擋 .env;若你改了路徑,請自己確保 token 不會被追蹤。

AIASE_LITELLM_KEY=<YOUR_TOKEN>
